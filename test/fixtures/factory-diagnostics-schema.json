{
  "diagnostic_schema": "aiterm-mcp.factory-diagnostics.v1",
  "top_level_fields": [
    "diagnostic_schema",
    "version",
    "overall",
    "mcp",
    "pty_list",
    "runtime_error_store",
    "vendor_dependencies"
  ],
  "status_values": ["ready", "not_applicable", "unverified"],
  "mcp": {
    "transport": "stdio",
    "initialize": "ready",
    "tool_call": "ready"
  },
  "mcp_fields": ["transport", "initialize", "tool_call"],
  "pty_list": {
    "access": "read_only"
  },
  "pty_list_fields": ["access", "status", "session_count"],
  "runtime_error_store_fields": ["status", "collection", "record_count", "unacknowledged_count"],
  "vendor_keys": ["claude", "codex", "grok"],
  "vendor_fields": ["status", "optional", "required_for"],
  "vendor_required_for": {
    "claude": ["claude_agent"],
    "codex": ["codex_agent"],
    "grok": ["grok_agent", "composer_agent"]
  }
}
